# Implementation Plans — mog memory optimization

Generated by the improve skill on 2026-08-06, against commit `60b6ee3e`
(branch `sapiex-patches`). Source: a 5-subagent deep memory audit (parse
retention, Yrs hydration, engine stores, viewport caches, bridge copies),
every planned finding re-verified in source by the advisor.

> **HASH MAPPING (2026-08-06)**: fork history was rewritten so every
> fork-added commit is authored+committed by Rohith K Rangan (pushed to
> buildwithrohith/mog). Trees are byte-identical; hashes changed. Key pairs:
> round-1 planning base `60b6ee3e` → `d42ad029`; round-1 integration merge
> `b4a3a3e5` → `80f56679` (current HEAD). Any hash cited in plans 001-007
> refers to pre-rewrite history; local branch `backup-pre-author-rewrite`
> preserves it for archaeology. Round-2 plans (008+) cite post-rewrite
> hashes only.

Executors: read your plan fully before starting, honor its STOP conditions,
run every verification, and update your row here when done. Plans live at
absolute path `/Users/vish/Repos/analyst/mog/plans/` (untracked in git — do
NOT commit this directory).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Lane | Status |
|------|-------|----------|--------|------------|------|--------|
| 001 | Sheet-switch clone elimination (O(N²) fix) | P1 | M | — | A (opt/deferred-diet) | DONE |
| 002 | Staging-peak diet (full-hydration copies) | P1 | M | 001 | A | DONE (verified post-004 by advisor; prior failure: test_xlsx_col_style_ranges::direct_cell_row_col_and_styled_blank_formats_export_through_xlsx) |
| 003 | Empty-Yrs landmine removal + export/sync guards | P0 | S-M | 001, 002 | A | DONE |
| 004 | Materialized-sheet style hydration (formats bug) | P1 | M | 001 (003 rec.) | A | DONE (focused gates pass; full-lib baseline exception documented below) |
| 005 | Viewport/palette sheet-switch cleanup wiring | P2 | S-M | — | B (opt/viewport-cleanup) | DONE |
| 006 | Parse-time string interning (SST dedup) | P1 | S-M | — | C (opt/parse-intern-observability) | DONE (baseline styles failure pre-existing, advisor-verified) |
| 007 | Memory observability (wasm sampling, tags, size report) | P2 | S | — | C | DONE (targeted gates pass; full compute-core lib has unrelated styled-blank fill-color failure) |
| 008 | Incremental per-sheet Yrs hydration design (ARCH-01) | P1 | L (design) | 001-007 | DESIGN | DONE (feasible with dependency-closure hydration; `idToPos` removal is a separate schema-v20 cutover) |
| 009 | Solid-fill color fallback (baseline fill defects) | P1 | S | — | D (opt/fill-color-fallback) | DONE (both fill-color baselines fixed; full parser/compute-core gates pass; three pre-existing fmt drifts remain) |
| 010 | CellData diet (boxed extras sidecar) | P1 | M | — | E (opt/celldata-diet) | DONE (600B→88B base + 528B boxed extras; repro-scale overhead 845MB→124MB; serde wire byte-identical; advisor-accepted 4d8b782b) |
| 011 | Formula-text dedup (Arc-shared scheduler caches) | P2 | S-M | 010 (lane order) | E | DONE (both caches Arc<str>, ptr_eq-proven after init+regen; legacy-clone sub-step no-op with evidence; advisor-accepted 07456c76) |
| 012 | CellValue shrink (box Image variant, 56B→~24B) | P1 | S-M | — | F (opt/cellvalue-shrink) | DONE (measured: CellValue 24B, CellEntry 32B, CellData 568B; serde wire format proven stable; advisor-accepted 84ebbd0f) |
| 013 | CF refresh once per mutation + clone elimination | P1 | M | — | G (opt/cf-and-patches) | DONE (3 CF evaluations per edit → 1, regression-proven; zero result-map clones; advisor-accepted fc0cb81e+3fadfe70) |
| 014 | Strip-based viewport format patches | P2 | S-M | 013 (lane order) | G | DONE (byte-identical fixtures; per-cell hash set removed; advisor-accepted b9801383+977d6095) |
| 015 | Incremental hydration impl, steps 1-3 of 008-DESIGN-OUTPUT (state sets, allocator authority, existing-sheet hydration primitive) | P1 | M-L | 008 | H (opt/incremental-hydration-1) | DONE |
| 016 | Parse allocation diet (CPU + transient churn) | P1 | M-L | — | I (opt/parse-alloc-diet) | DONE (Steps 1-5c landed: numeric provenance, shared-formula cache, SST Arc interning, validated UTF-8 conversion with invalid-UTF-8 boundary regression, projection-role fast path, workbook SST lookup reuse, exact lazy cell capacity; parser 3,377 passed, deferred compute 50 passed, doctests 7 passed/45 ignored, parallel feature check and cargo check --workspace --locked clean; fmt has only pre-existing bridge macro drift; A/B assigned to post-merge advisor) |
| 017 | Engine CPU scoping (O(N²) materialize fix + recalc wins) | P1 | M | — | J (opt/engine-cpu-scoping) | DONE (3-unit one-sheet index counter; 3023 compute-core tests pass; workspace check clean; 3(a) ownership STOP documented) |
| 018 | Native allocator return-to-OS (server RSS 13GB→~4GB) | P1 | S-M | — | K (opt/native-allocator) | DONE — VERIFIED ON PROD LINUX 2026-08-10 (jemalloc shipped in the 352fc89 cut, tikv-jemallocator disable_initial_exec_tls, compute/napi/Cargo.toml:47; Railway prod memory metrics show return-to-OS on real workbooks: spike 9.9GB -> settled 3.8GB in ~2min (Aug 9 11:19Z), 8.5GB -> 3.1GB in 3min (Aug 10 04:22Z), 6.6GB -> 4.3GB in 2min (Aug 10 08:31Z); V2 acceptance bar post-drop <=4GB met in production; macOS footprint ambiguity superseded by Linux ground truth) |
| 019 | Fast-path parse robustness (silent truncation/misplacement/SST loss) | P0 | M | — | L (opt/parse-robustness) | DONE (diagnostics sink; safe malformed-cell resync; invalid r= skipped/reported; invalid SST uses #REF!; clean output parity and zero diagnostics; workspace check clean; xlsx-parser 3375 passed; deferred-import 50 passed; commits e717fa0e, 25117c11, 7dcbf5d2, 8d590773, a3190843) |
| 020 | Incremental snapshot lowering (last per-click O(cumulative) cost) | P1 | L | 017 | N (opt/incremental-lowering) | DONE (incremental target-sheet lowering for preview and durable hydration; counter regression [1,2,2]; workspace check clean; compute-core 3,024 + deferred 52 passed; xlsx-parser 3,381 unit tests plus integration/doc tests passed; commits d4d2c89a, bc04efbc, 167e603b, 2bf88a42) |
| 021 | Round-4 small wins (padding bound, style-run diagnostics, batched #REF!) | P2 | M | — | O (opt/round4-small-wins) | DONE (compute-core 3,024 passed; xlsx-parser 3,382 unit tests plus integration/doctests passed; bridge generation and compute-core queries 38 passed; kernel typecheck blocked by missing workspace declarations; commits 38ee3008, 0745fb06, 12ff2567, 724ded91) |
| 022 | Dependency-closure hydration (008 §5 row 4, ARCH riskiest step) | P1 | L | 015 | M (opt/dependency-closure, sol) | DONE (`263707bf`, `2dc59095`, `f80fdf63`; compute-core 3033/3033) |
| 023 | Remove persisted `idToPos` via schema-v20 compacting cutover | P1 | L | 008 | P (opt/schema-v20-cutover) | DONE (`f2655d5c`, `ed7a5928`; lookup-before-mint identity fix; compute-core 3052/3052; compute-document 277/277 + 8/8; provider 35/35; 1,000-cell `encode_full_state` 275,061 → 167,030 bytes, -39.28%) |
| 024 | Mirror positional-map merge (lazy reverse index) | P2 | M-L | — | Q (opt/mirror-bimap) | DONE (accessor-routing and representation commits `52e4dfd8`, `cd3f1e85`; compute-core 3,036/3,036 and mirror 181/181) |
| 025 | Binary wire format for bulk range reads | P2 | L | — | R (opt/binary-wire) | DONE (WASM bytes-tuple STOP passed; pure-numeric 10k 260,047/899,407 = 28.91%, 71.09% reduction; mixed 10k 438,950/1,568,291 = 27.99%; commits `0971128f`, `09c9e42d`, `203e5439`, `7a2d5fa7`) |

| 026 | Identity-free binary wire variant for value/format reads | P2 | M | 025 | T (opt/values-only-wire) | DONE (already satisfied by round-5 values/formats siblings; dense 10k values 80,027/141,585 = 56.52%, formats 30,055/290,201 = 10.36%; query identity baseline 260,047/899,407 = 28.91%; no remaining eligible identity-carrying viewer caller; compute-core 3,060 passed + 1 ignored; wire Jest 321 passed; bridge regen stable; no commit); advisor concurred: remaining identity-carrying read (validation queryRange) is viewport-bounded, new codec variant not worth the surface |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

## Dependency notes

- Lane A is strictly sequential (001 → 002 → 003 → 004): all four edit
  `construction/deferred.rs`.
- Lanes B and C are independent of A and of each other (disjoint files).
- 007's only touch on `deferred.rs` is adding an argument at existing
  `install_observer(` call sites — if lane A has that file in flight, land A
  first and rebase (trivial).

## Held follow-ups (deliberately NOT planned yet)

- **Mirror double-store** (`cells` + `col_data` duplicate every CellValue,
  `mirror/write/cells.rs:119,175`): largest steady-state multiplier, M-L/MED —
  gated on plan 007's measured sizes.
- **CellData shrink** (~500-600B fixed/cell estimated): L/HIGH — gated on 007's
  `size_of` report.
- **Yrs `idToPos` removal / key packing** (~40-50% of per-cell Yrs cost):
  L/HIGH — document-schema change + migration; gated on measurements and a
  design pass.
- **Incremental per-sheet Yrs hydration** (ARCH-01): blockers inventoried
  (single transaction, ID allocator coupling, observer/undo suppression —
  the suppress mechanisms already exist); L/HIGH — design task for a
  judgment-tier model AFTER the diet plans land and 007 numbers exist.
- **Materialized-sheet LRU eviction**: requires kernel materialization-tracker
  coordination (Rust-side eviction alone would make the kernel skip
  re-materialization and show blank sheets). Needs a cross-boundary design.
- **Binary wire format for large range reads** (N-API JSON→binary): L/MED,
  server-path win.
- **`executeHydrateXlsx` extra byte copy** (kernel:2161-2178): S but
  intent-unclear (may guard against caller mutation) — investigate before
  removing.
- **CF-cache double clone per recalc; formula-text triplication; viewport
  delta per-cell merge; unbounded row-identity allocation**: round-2
  candidates, each S-M, none gating.

## Known baseline test failures (pre-existing, NOT caused by these plans)

- None currently. The two fill-color failures below were fixed by plan 009.

## Fixed by plan 009

- `xlsx-parser` `roundtrip_parse_output` `styles::roundtrip_fill_formatting`
  and `compute-core --lib`
  `test_xlsx_col_style_ranges::direct_cell_row_col_and_styled_blank_formats_export_through_xlsx`.
  Both passed after the solid-fill foreground fallback was added; the
  background-color priority contract and colorless-solid style identity remain
  covered by `fill_lowering` tests.

## Findings considered and rejected

- Freeing `raw_xlsx_bytes` early inside staging: staging holds `&self` by
  contract (failed staging must leave the engine retryable); rejected.
- Dropping `raw_xlsx_bytes` on the read-only path: it is the re-parse source
  for on-demand sheet materialization; load-bearing, not waste.
- Per-sheet `tracing` breadcrumbs: once per sheet, negligible; keep (they
  located the original OOM).
- AST cache eviction: verified correct (removed on delete/clear/sheet-delete);
  not a leak.
